<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gms.mapper.StudentMapper">
    <select id="selectByStuIdWithInfo" resultType="com.gms.entity.Student">
        SELECT s.*, c.class_name AS className, m.major_name AS majorName, d.dept_name AS deptName
        FROM student s
        LEFT JOIN class c ON s.class_id = c.class_id
        LEFT JOIN major m ON c.major_id = m.major_id
        LEFT JOIN department d ON m.dept_id = d.dept_id
        WHERE s.stu_id = #{stuId}
    </select>
</mapper>
